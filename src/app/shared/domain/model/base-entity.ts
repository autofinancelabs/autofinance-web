/**
 * Defines a standard structure for entities with a unique identifier.
 */
export interface BaseEntity {
  /**
   * The unique identifier for the entity (a backend-assigned UUID).
   */
  id: string;
}
